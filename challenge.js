//Question 1
let balance = 100
const initialPrompt = prompt('How may I help you today ?')

if (initialPrompt === 'Deposit Money') {
    prompt('How Much would you like to deposit?');
} else if(initialPrompt === 'Withdraw Money') {
    prompt('How much would you like to withdraw ?')
}

if(deposit === 'Deposit Money') {
    prompt('How much would you like to deposit')
} else if(withdraw == 'Withdraw Money') {
    prompt('How much would you like to withdraw')
}
